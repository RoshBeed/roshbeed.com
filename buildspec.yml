version: 0.2
phases:
  install:
    commands: 
      - gem install bundler
      - bundle install
  build:
    commands:
      - jekyll build --trace
  post_build:
    commands:
      - ls -la _site/
artifacts:
  files:
    - '**/*'
  base-directory: _site/
